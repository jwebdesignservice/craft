# 2026-03-17 — Daily Log

## Memory Hub (#memory-hub channel)

### Local Dev Setup
- memory-hub frontend: `C:\Users\Jack\Desktop\AI Website\htdocs\Websites\memory-hub\frontend` (Next.js 14, port 3000)
- memory-hub backend: `C:\Users\Jack\Desktop\AI Website\htdocs\Websites\memory-hub\backend` (FastAPI, port 8002 local — no local DB so falls back to demo data)
- memory-market-dev frontend was already running on port 3000; memory-hub bumped to 3001 initially causing CORS issues

### Bug Fix: Marketplace showing no memories
- Root cause: `next.config.js` had hardcoded fallback `NEXT_PUBLIC_API_URL: 'http://localhost:8000'` which **overrode** `.env.local` value
- Browser was hitting localhost:8000 (nothing running) instead of Railway backend
- Fix: Changed fallback in `next.config.js` to `https://memory-market.up.railway.app`
- Also added `localhost:3001` and `127.0.0.1:3001` to Railway backend CORS allowlist in `memory-hub/backend/src/main.py`
- Railway backend has 28 memories, all returning correctly from `/memory/search`

### Key Distinction
- **memory-market-dev** = the original MemoryMarket project (memory-market.vercel.app)
- **memory-hub** = rebranded clone of memory-market for separate deployment
- wils clarified: #memory-hub channel is for the memory-hub project, NOT memory-market

### memory-hub REBRAND-PROGRESS.md
- Homepage complete (all sections redesigned with orange/black theme)
- Components redesigned: Hero, Header, Problem, Solution, HowItWorks, MemoryTypes, Pricing, Footer
- Still TODO: NetworkCanvas, WalletButton, WalletProvider polish; backend credential replacement; new GitHub repo
- Design: Orange #FF6B00 primary, dark backgrounds, Space Grotesk + Inter fonts, 0px border-radius on cards

### Running Servers (as of 14:17)
- memory-market-dev frontend: port 3000 (session amber-nudibranch) — later killed when memory-hub took port 3000
- memory-market-dev backend: port 8001 (session lucky-canyon)
- memory-hub frontend: port 3000 (session plaid-summit)
- memory-hub backend: port 8002 (session salty-daisy — no local DB, warning logged)
