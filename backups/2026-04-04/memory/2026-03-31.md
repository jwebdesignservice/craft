# 2026-03-31 — WedScout MVP Build

## Project
- **WedScout** — wedding vendor directory for Texas
- **Location:** C:\Users\Jack\Desktop\AI Website\htdocs\Websites\wedscout
- **GitHub:** https://github.com/jwebdesignservice/Wedscout
- **Client:** External (wils is building for a client who will be the admin)

## Stack
- Next.js 16 + Tailwind CSS + Framer Motion + Lucide React
- Supabase (auth + DB) — credentials in `.env.local`
- Supabase project URL: `https://tpkrxguutlrtdgfkcvao.supabase.co`

## Design System
- Cream: #FFF4E2 (primary bg)
- Cream-dark: #F7E9D4 (secondary bg)
- Charcoal: #1A1A1A (text, dark sections)
- Green: #2B895A (CTAs, accents)
- Green-dark: #1F6944 (hover states)
- Fonts: Cormorant Garamond (headings), Poppins (body)

## What Was Built Today
- Full homepage with Hero, Categories, Featured Vendors, How It Works, Testimonials, CTA, Footer
- `/vendors` — directory with filters (category, price, rating, location)
- `/vendors/[id]` — vendor profile pages with enquiry form
- `/categories` — category browser
- `/about` — mission, stats, team
- `/contact` — redesigned with proper hero, form, FAQ accordion
- Auth system: `/login`, `/signup`, `/forgot-password`, `/dashboard`, `/account`
- Admin panel: `/admin/vendors` with full CRUD (add/edit/delete vendors)
- 404 page

## Key Decisions
- **Demo mode:** Admin CRUD uses localStorage (not Supabase) so client can demo without DB setup
- **Shared vendor store:** `lib/vendor-store.ts` — admin changes reflect across public site instantly via localStorage + custom events
- **Supabase schema exists** (`supabase/schema.sql`, `supabase/vendors-schema.sql`) — ready to switch to real DB when going live
- **Admin role check disabled** in middleware for demo — re-enable later

## Current State
- Hero search now redirects to `/vendors?search=X&location=Y` and pre-fills inputs
- Images using picsum.photos (Unsplash was failing to load)
- User needs to clear localStorage if seeing broken images: `localStorage.removeItem('wedscout_vendors')`

## Still TODO (for later)
- Re-enable Supabase vendors CRUD (currently local storage only)
- Fix RLS policies for vendor management
- Re-enable admin role check in middleware
- Google OAuth callback setup
- Blog page
- Pricing page for vendors
- Vendor comparison tool
- Image upload to Supabase Storage (currently using URLs)

---

## Cron Optimisation (2026-03-30)

### Fixes Applied
1. **Paperclip Heartbeat** — Removed 11 hardcoded API keys from prompt (~500 tokens). Agent now reads keys from `C:\Users\Jack\.env.george` at runtime. Saves ~12k tokens/day.

2. **Nightly agent prompts slimmed:**
   - Primrose: ~2,000 → ~150 tokens (reads `AGENT-BRIEF.md` instead of duplicating)
   - Desert Falcons: ~2,500 → ~150 tokens (same pattern)
   - Combined: ~9k tokens/night saved

3. **Backup cron fixed** — Was failing due to isolated session trying to post to Discord (permissions conflict). Now runs silently on success, posts to #errors only on failure.

### Paperclip Agent Key Mapping (for reference)
```
PAPERCLIP_DEV → f93dc400
PAPERCLIP_COPYWRITER → 861e3ef0
PAPERCLIP_SCRIPTWRITER → 34403575
PAPERCLIP_SOCIAL → 42dbde88
PAPERCLIP_SEO → af5632b0
PAPERCLIP_MARKETING → 4b295d83
PAPERCLIP_ADS → 39dac44c
PAPERCLIP_OUTREACH → 750e1aeb
PAPERCLIP_ANALYTICS → d087e120
PAPERCLIP_VIDEO → 1ae5bdf9
PAPERCLIP_VISUAL_DIRECTOR → 35d45b16
```

### Channel Cleanup Completed (2026-03-30)
- #primrose-ever-care, #desert-falcons, #fibbot, #fast-launch, #oracle, #clausekit — all purged with handovers posted
- Token warning cron created: `a4e70006-f14f-4ebd-bb4b-a89ed5dc943c` — runs daily 9am, alerts if #george session >250k or any session >200k tokens
