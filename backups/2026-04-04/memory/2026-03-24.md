# Memory — 2026-03-24

## Fast Launch Project (Jack & Dil MVP Site)

### Repo & Local Path
- GitHub: https://github.com/jwebdesignservice/Jack-and-Dil-MVP-Site
- Local: C:\Users\Jack\Desktop\AI Website\htdocs\Websites\fast-launch
- Vercel project: jack-and-dil-mvp-site

### Stack
- Next.js 14 (App Router, TypeScript, Tailwind)
- Framer Motion for animations
- shadcn (initialised), Playwright (installed)
- Custom Button component: components/ui/Button.tsx (primary + outline variants with techy hover effects)

### Design System
- Background: #000000 (black), inverted sections use #0A0500
- Primary: #F97316 (orange-500)
- Container max-width: 1200px (custom in tailwind.config.ts)
- Font: Inter via next/font/google, font-semibold for headings (not font-bold)
- Section labels: text-xs font-bold tracking-[0.2em] uppercase

### Layout
- Two fixed vertical orange lines (1px, rgba(249,115,22,0.20)) at calc(50% - 640px) from each side — defined in app/layout.tsx

### Sections built (all in components/sections/)
- Hero.tsx — dashboard graphic (HeroDashboard), animated badge, two CTAs
- Marquee.tsx — single row, infinite, py-5 padding
- Services.tsx — 2×2 grid, custom SVG icons, GlowCard
- Comparison.tsx — inverted #0A0500 bg, orange grid, VS badge, speed bar
- Process.tsx — vertical alternating layout, text left/SVG right alternating, 4 colourful SVGs (scope/design/build/launch), center node dots on absolute positioned vertical line
- About.tsx — single unified card (border + bg #0A0A0A), left = GitHub-style team activity card (commit graph, team members, recent commits, stats), right = text + tech tags + CTA. Inverted section bg #0A0500.
- Pricing.tsx — 3 cards, CountUp animation, trust badges
- Testimonials.tsx — dual scroll columns
- FAQ.tsx — custom accordion
- CTA.tsx — circuit board SVG bg, animated gradient blobs, corner brackets

### Hero Dashboard SVG
- File: components/svgs/HeroDashboard.tsx
- Content: bar chart, line/area chart, donut ring, stat cards, floating notification card, floating deploy badge

### Process SVGs
- ScopeSVG: reticle/crosshair with blue corner brackets
- DesignSVG: browser wireframe with traffic lights
- BuildSVG: terminal with coloured output + animated progress bar
- LaunchSVG: globe + animated rocket with coloured exhaust + orbit arc

### About Section
- Left: team activity card — JW (orange) + DM (blue) devs, commit heatmap, 3 recent commits, 4 stats row
- Right: text content with tags and CTA button
- Both inside one shared card: border border-[rgba(249,115,22,0.2)] bg-[#0A0A0A]

### Button Component (components/ui/Button.tsx)
- Primary: scan line sweep + corner brackets + orange glow on hover, arrow slides right
- Outline: dual scan lines top/bottom + corner brackets grow in + background tints on hover
- All buttons across site use this component

### Known Issues / Dev Notes
- `.next` cache corrupts frequently when making edits with dev server running — must `Remove-Item -Recurse -Force .next` and restart
- Stop-Process -Name node causes exec tool to hang — user must kill node manually
- Container padding: all sections use `container mx-auto px-6`

### Last Git Commit
- "feat: full redesign — Next.js 14, black/orange theme, dashboard graphic, all sections rebuilt"
- On main branch, pushed to origin

### Pending / Not Yet Done
- No Vercel deploy yet (user hasn't requested)
- GOTCHAS.md / AGENT-BRIEF.md not yet written for this project
- shadcn components not yet used (initialised but not integrated into components)
