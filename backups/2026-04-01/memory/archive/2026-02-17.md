# 2026-02-17 Memory Log

## Polymarket Bot Analysis (JMoon)

### Issue Reported
- JMoon reported polymarket-bot non-responsive in #polymarket-bot channel
- Both polymarket-bot cron jobs are **disabled**
- Config binding exists for channel `1472255916639522930` but no active session
- Gateway restart is disabled (`commands.restart=true` not set)

### Critical Finding: v9.5 vs v10.2 Difference

**v9.5 achieved ~76% win rate. v10.2 dropped to ~41%.**

The key difference: **DOWN BIAS is missing from v10.2**

v9.5 had this logic (lines 265-268):
```python
# DOWN BIAS: If confidence < 55%, default to DOWN
# Based on data: DOWN wins 77%, UP wins 29%
if confidence < 0.55:
    direction = 'DOWN'
```

v10.2 removed this entirely.

### Other Differences
1. v10.2 clears `price_history` between rounds - v9.5 keeps continuous data
2. v10.2 uses `float(slot)` as round_start_time - v9.5 uses `time.time()`

### v9.5 Winning Components
- 20s entry delay (wait for momentum)
- 4-signal voting: PRICE, SHORT_MOM, MED_MOM, ACCEL
- Skip trades when confidence < 0.5 (return 0 shares)
- DOWN bias when uncertain (exploits 77% DOWN win rate)
- Dynamic position sizing: 15 shares high conf, 10 medium, 0 low

### To Fix v10.2
1. Add DOWN bias back
2. Don't clear price_history between rounds
3. Use actual time, not slot time for round_start

### Open Question
Need to verify "DOWN wins 77%" is still valid - may have been measured during specific market conditions.

### Files
- v9.5: `polymarket-bot/live_trader_v9.5_momentum.py`
- v10.2: `polymarket-bot/live_trader_v10.2_fixed.py`
- Trades log: `polymarket-bot/logs/trades.jsonl`
