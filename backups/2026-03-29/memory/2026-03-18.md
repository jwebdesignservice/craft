# 2026-03-18 — Daily Log

## Agent Hub (#ai-hub channel) — Major Session

### DB Connection Fix
- Root cause: Railway backend using direct Supabase connection string (`db.xxx.supabase.co:5432`) which was failing silently
- Fix: Switched to Supabase **session pooler** string (`aws-1-eu-west-1.pooler.supabase.com:5432`)
- DB host: `db.sbnntgbkckybnxjtpmhj.supabase.co` → pooler at `aws-1-eu-west-1.pooler.supabase.com:5432`
- Supabase project: `sbnntgbkckybnxjtpmhj` (devstack-design / dev.stack999@gmail.com)
- This fixed: listings not persisting, marketplace showing demo data, creates returning `persisted: false`

### Railway Service Confusion (IMPORTANT)
- **Correct Railway service URL:** `agent-hub-production-a36e.up.railway.app`
- wils was accidentally redeploying a DIFFERENT Railway service for ~1 hour
- The wrong service had no DATABASE_URL set, logs showed "DATABASE_URL not set, using localhost"
- Lesson: Always verify the Railway service URL matches before redeploying

### SSL Note
- Supabase pooler REQUIRES `ssl.CERT_NONE` — strict SSL verification breaks the connection
- Attempted `ssl.create_default_context()` without CERT_NONE → returned 0 memories, `database-error`
- Reverted immediately — CERT_NONE is required for this setup

### Features Built/Fixed
- Edit listing: wallet auth + proper error handling (was showing `[object Object]`)
- Free purchases: skip on-chain tx entirely, just record access grant (then updated to require wallet signing after auth audit)
- EDIT button added to MemoryCard (dashboard Created/Listed tabs)
- RATE button (red) on acquired cards in dashboard
- 4-dimension review modal: Accuracy, Relevance, Completeness, Usefulness (1-5 each)
- Hero CTAs linked to /marketplace and /docs (were dead buttons)
- SVG encoding fix: broken character `�` on line 71 of step-02-browse.svg
- HowToApply tab styling: both Python/JS tabs visible with orange theme
- `is_listed` field added to MemoryResponse + frontend Memory interface
- Vercel proxy: `force-dynamic` + `revalidate = 0` to prevent edge caching
- `next.config.js` and `api.ts` server-side fallback URLs updated to agent-hub Railway

### Fresh Devnet Wallets Generated
- **Treasury (4%):** `yuGgS52D7x9ck18Ce3JKJC2FuEjpVFnp9pVyi9BDUdh`
- **Stake Pool (1%):** `AcWjA5EpqjKLrAoPQhfAC2dSjAYeoQbkVMCxtB9yjYjY`
- Private keys saved in `memory-hub/WALLETS-DEVNET.md` (gitignored)
- Old wallets: Treasury `7URz...gxUn`, Stake Pool `2Kt9...QBe` (deprecated)

### OG MemoryMarket Skills Cleanup
- 11 overlapping skills removed from Agent Hub DB
- Added `_OG_SKILL_IDS` blocklist in search query (`~Memory.id.in_(...)`) as safety net
- Added `/memory/cleanup-og` endpoint (admin-only) for manual cleanup
- Seed file runs cleanup on every startup before seeding
- Cascade deletes: quality_stakes, performance_reports, access_grants, memory_embeddings, listings, then memories

### Security Audit — 25 Issues Found
- **6 CRITICAL** (all fixed): API key in health, cleanup-og no auth, purchase/stake/unstake/claim no wallet verification
- **6 HIGH** (2 fixed): free purchase signing, ghost endpoint secured. Remaining: webhook HMAC, nonce store, IPFS, fee split docs
- **7 MEDIUM** (6 fixed): N+1 query, SQL pagination, DB indexes (is_listed + domain_tags GIN), dead code, duplicate CORS, dead webhook function. SSL revert needed.
- **6 LOW**: enum naming, timezone inconsistency, old route redundancy

### Backend Architecture Notes
- Frontend uses `/api/proxy` route on client-side → hits Railway backend
- Server-side rendering uses `NEXT_PUBLIC_API_URL` env var (set on Vercel) or fallback in next.config.js
- Railway auto-deploys from GitHub (devstack-design/Agent-hub repo, `backend` root directory)
- Vercel auto-deploys from same repo (frontend directory via framework detection)
- Custom start command on Railway: `uvicorn src.main:app --host 0.0.0.0 --port $PORT`

### Vercel Frontend URLs
- Production: `agent-hub-tau.vercel.app`
- Also in CORS allowlist: `memory-hub-gules.vercel.app` (old name, 404 now)

### Current State (end of session)
- 24 memories on marketplace (all Agent Hub originals + user-created)
- 0 OG overlap skills
- DB connected and healthy via pooler
- All critical auth issues fixed and verified live
- Phase 3 medium fixes deployed, pending SSL revert deploy verification
