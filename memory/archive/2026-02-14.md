# 2026-02-14 - Polymarket Bot

## Polymarket Trading Bot
- Built full Python bot: `polymarket-bot/` in workspace
- Strategy: Leg 1 (buy dumped side) + Leg 2 (hedge opposite) = ~5% guaranteed profit
- Parameters: shares, sum=0.95, move=0.15, windowMin=2
- CLI: `auto on <shares> [sum=0.95] [move=0.15] [windowMin=2]`
- trader.py is stub only — needs Polymarket API key + Polygon wallet + USDC
- Discussed BTC volatility filter (pause bot if BTC moves >2% in 1 min) — not yet built
- User still needs: Polymarket account, Polygon wallet, USDC funding, VPS

## Declined Requests
- Refused to build "Blackification" tool — skin darkening AI tool targeting people by race
- Refused twice even when reframed

## Polymarket Bot - Additional Details
- Backtest system built: `polymarket-bot/backtest/` — 5 files
- Run: `cd polymarket-bot/backtest && pip install -r requirements.txt && python run_backtest.py`
- Tried to install Python via winget — failed (needs admin/elevated perms not available from Discord)
- User needs to install Python manually from python.org (tick "Add to PATH")
- Once Python installed, backtest can run locally OR I can run it via sub-agent
- BTC volatility filter discussed but NOT yet built — user interested

## Notes
- User's workspace actual folder name: "AI Websites" (with S) on disk
- Config/workspace path used: "AI Website" (without S) — may cause issues
- BOOTSTRAP.md still exists in workspace — not deleted yet
- Python NOT installed on machine yet
